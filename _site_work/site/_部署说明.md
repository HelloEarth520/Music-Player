# MusicPlayer 站点部署说明（v2.22.18）

## ⚠️ 上次传错的原因（务必先看）

今天早上那次上传，把 `web-demo/index.html` 放到了**站点根目录**，
把原来的介绍页覆盖掉了 —— 线上 `https://20100510.xyz/` 现在打开是播放器演示页，
不是介绍页。

对照证据：线上根页返回 **41888 bytes**，与 `web-demo/index.html` 完全一致；
且线上根页 title 是 `MusicPlayer`（demo），而介绍页 title 是
`MusicPlayer - 一切皆可自定 · 跨平台本地音乐播放器`，section.page 数量为 0（介绍页应为 4）。

## ✅ 正确的目录结构（上传时必须保持）

```
站点根目录/
├── index.html           ← 介绍页（30908 bytes，4 屏横滑落地页）
└── web-demo/            ← 子目录，不能提到根
    ├── index.html       ← 播放器演示页（41888 bytes）
    ├── demo.js          ← 演示逻辑（84647 bytes）
    └── style.css        ← 样式（87948 bytes）
```

**别把 `web-demo/` 里的文件挪到根目录**——介绍页与演示页是两个不同页面，
介绍页里的「Web 版演示」按钮链接指向 `web-demo/index.html`。

## 上传方式

把本 zip 解压后的内容**整体覆盖**服务器站点目录（保留 `web-demo/` 这一层）：

```bash
unzip MusicPlayer-website-v2.22.18.zip -d /你的站点目录/
```

覆盖后先自检：
1. `https://20100510.xyz/` → 打开应是**介绍页**（4 屏横滑、底部有免责声明）；
2. `https://20100510.xyz/web-demo/` → 打开应是**播放器演示页**。

## 本版同步内容（v2.22.18）

- **介绍页**：版本号 v2.22.12 → **v2.22.18**（首屏徽章 + 下载卡片两处）；
  其余文案、4 屏结构、霓虹免责声明确认完好未动。
- **web-demo（演示页）**：版本号 v2.22.17 → **v2.22.18** 对齐主端；
  功能面已与主端 v2.22.17 齐平（目录折叠 / 定位当前播放 + 可调滚动时长 /
  封面左右滑切歌 / 主界面元素显隐 ×4 / 布局调节 / 炫彩分区三区独立开关 + 自定义颜色 /
  外观设置持久化 `mp_appearance`）。

## 本地冒烟验证（打包前已跑）

用 headless Chromium 实测三件套：
- 介绍页：title 正确、4 个 section.page、2 个 demo 链接、正文可见 → 正常；
- 演示页：title `MusicPlayer`、version 显示 **v2.22.18**、titlebar/playlist/cover/controls/dirHeader 元素齐全；
- 两个页面 **JS 错误数 = 0**。
